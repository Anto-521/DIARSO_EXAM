@model Movistar.Models.Distrito

@{
    ViewData["Title"] = "Distritos";
}

<h1>Edit</h1>

<h4>Distrito</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CodDis" class="control-label"></label>
                <input asp-for="CodDis" class="form-control" />
                <span asp-validation-for="CodDis" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="NombreEs" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
